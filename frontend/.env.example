# Production / Vercel — point at the Render backend.
# Copy to frontend/.env.local (or set in the Vercel project env) for builds.
VITE_API_BASE_URL=https://adaptive-backend-qmc1.onrender.com
VITE_WS_BASE_URL=wss://adaptive-backend-qmc1.onrender.com
